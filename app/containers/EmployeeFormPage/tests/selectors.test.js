// import { selectEmployeeFormPageDomain } from '../selectors';

describe('selectEmployeeFormPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
